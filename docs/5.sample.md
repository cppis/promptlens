# Promptic — 사용 샘플 모음 (v1.1)

> 실제 대화 흐름을 기반으로 한 사용 시나리오
> 각 샘플은 독립 실행 가능하며, 앞 샘플의 결과를 이어받는 형태로도 사용 가능

---

## 샘플 1. 첫 시작 — 설정 및 프로젝트 생성

### 1-1. 현재 설정 확인

**사용자:**
```
설정 확인해줘
```

**도구 호출:** `get_settings`

**예상 응답:**
```
현재 설정
─────────────────────────────
API 키:        미등록 (local 모드만 사용 가능)
모델:          claude-haiku-4-5
활성 프로젝트:  없음
저장 경로:     ~/.promptic/data
```

---

### 1-2. API 키 등록

**사용자:**
```
API 키 등록해줘. sk-ant-api03-...
```

**도구 호출:** `set_api_key`

```json
{
  "apiKey": "sk-ant-api03-...",
  "model": "claude-haiku-4-5"
}
```

**예상 응답:**
```
✅ API 키가 등록되었습니다.
모델: claude-haiku-4-5
api 모드 및 deep 모드 사용 가능합니다.
```

---

### 1-3. 프로젝트 생성 및 활성화

**사용자:**
```
"게임 서버 프롬프트" 프로젝트 만들어줘
```

**도구 호출:** `create_project`

```json
{ "name": "게임 서버 프롬프트" }
```

**사용자:**
```
방금 만든 프로젝트 활성화해줘
```

**도구 호출:** `set_active_project`

```json
{
  "action": "set",
  "projectName": "게임 서버 프롬프트"
}
```

**예상 응답:**
```
✅ "게임 서버 프롬프트" 프로젝트가 활성화되었습니다.
이후 analyze_prompt 결과가 자동으로 이 프로젝트에 저장됩니다.
```

---

## 샘플 2. 기본 분석 — local 모드

### 2-1. 초안 프롬프트 분석

유저가 Go 서버 코드 리뷰 요청 프롬프트를 처음 작성한 상황.

**사용자:**
```
이 프롬프트 분석해줘

---
코드 리뷰해줘.
---
```

**도구 호출:** `analyze_prompt`

```json
{
  "prompt": "코드 리뷰해줘.",
  "mode": "local"
}
```

**예상 응답:**
```
분석 결과 (local 모드)
─────────────────────────────
종합 점수: 22 / 100   등급: D

5축 점수:
  clarity        ██░░░░░░░░  20
  completeness   ██░░░░░░░░  18
  specificity    ██░░░░░░░░  20
  context        █░░░░░░░░░  15
  output_format  ██░░░░░░░░  18

누락된 요소:
  - 리뷰 대상 코드 없음
  - 언어/프레임워크 미지정
  - 리뷰 기준 없음 (성능? 보안? 가독성?)
  - 출력 형식 미지정

entryId: `h_a1b2c3d4e5_f6g7h8`
```

---

### 2-2. 개선 후 재분석 (`>> re` 활용)

**사용자:**
```
이 프롬프트 분석해줘 (parentId: 직전 버전)

---
당신은 Go 백엔드 코드 리뷰어입니다.
아래 코드를 검토하고 다음 기준으로 피드백을 주세요:
1. Go 관용 패턴 준수 여부 (에러 처리, 인터페이스 설계)
2. 보안 취약점 (SQL injection, 인증 누락)
3. 성능 문제 (goroutine 누수, 불필요한 할당)

출력 형식:
- 심각도별 분류 (🔴 critical / 🟡 warning / 🟢 suggestion)
- 각 항목마다 코드 예시 포함

```go
[코드 삽입 위치]
```
---
```

**도구 호출:** `analyze_prompt`

```json
{
  "prompt": "당신은 Go 백엔드 코드 리뷰어입니다...",
  "mode": "local",
  "parentId": "h_a1b2c3d4e5_f6g7h8"
}
```

**예상 응답:**
```
분석 결과 (local 모드)
─────────────────────────────
종합 점수: 84 / 100   등급: A   (이전 대비: +62 ▲)

5축 점수:
  clarity        ████████░░  82
  completeness   █████████░  88
  specificity    █████████░  90
  context        ████████░░  80
  output_format  ████████░░  82

누락된 요소:
  - 없음

entryId: `h_z9y8x7w6v5_u4t3s2`
```

---

## 샘플 3. API 모드 — 3색 리포트

API 키가 등록된 상태에서 더 깊은 분석을 원할 때.

**사용자:**
```
이 프롬프트 API 모드로 분석해줘

---
Unity 게임에서 플레이어 이동 시스템을 구현해줘.
WASD 입력을 받고, 물리 기반으로 움직이며, 벽과 충돌 처리도 포함해줘.
C#으로 작성해줘.
---
```

**도구 호출:** `analyze_prompt`

```json
{
  "prompt": "Unity 게임에서 플레이어 이동 시스템을 구현해줘...",
  "mode": "api"
}
```

**예상 응답:**
```
분석 결과 (api 모드)
─────────────────────────────
종합 점수: 61 / 100   등급: C

5축 점수:
  clarity        ██████░░░░  62
  completeness   ██████░░░░  58
  specificity    ██████░░░░  60
  context        █████░░░░░  55
  output_format  ███████░░░  68

3색 요소 분류:

  ✅ Referenced (명시된 요소)
     - 입력 방식: WASD
     - 물리 기반 이동
     - 벽 충돌 처리
     - 언어: C#

  🔶 Inferred (추론된 요소)
     - Unity 버전: 현재 LTS 추정
     - 이동 방식: CharacterController 또는 Rigidbody
     - 카메라 연동 여부: 불명확

  ❌ Missing (누락된 요소)
     - 이동 속도/점프 여부 미지정
     - Update vs FixedUpdate 물리 처리 기준 없음
     - 스크립트 구조 (MonoBehaviour 단독 vs 컴포넌트 분리) 미지정
     - 모바일/PC 구분 없음

개선 제안:
  "Unity 2022 LTS, C#. FixedUpdate 기반 Rigidbody 이동.
   이동 속도: 5f, 점프 없음. 컴포넌트 분리 (InputHandler / MovementController).
   PC 전용."

entryId: `h_p1q2r3s4t5_u6v7w8`
```

---

## 샘플 4. 버전 체인 — 반복 개선 워크플로우

### 4-1. v1 분석

**사용자:**
```
>> 분석

---
AWS Lambda 함수 코드 작성해줘.
---
```

**예상 응답:**
```
종합 점수: 18 / 100   등급: D

entryId: `h_lam1_v1xxxx`
```

---

### 4-2. v2 분석 (`>> re` 단축 트리거)

**사용자:**
```
>> re

---
당신은 AWS Lambda + Go 전문가입니다.
아래 요구사항의 Lambda 함수를 작성해주세요:

기능: SQS 메시지를 받아 DynamoDB에 저장
런타임: Go 1.21 (provided.al2)
에러 처리: 부분 실패 시 실패한 메시지 ID만 반환 (BatchItemFailures)
환경변수: TABLE_NAME (DynamoDB 테이블명)
IAM: DynamoDB PutItem 권한만 필요

출력: 실행 가능한 main.go + 유닛 테스트 포함
---
```

**도구 호출 (내부):** `analyze_prompt` with `parentId: "h_lam1_v1xxxx"`

**예상 응답:**
```
종합 점수: 91 / 100   등급: A   (이전 대비: +73 ▲)

entryId: `h_lam1_v2yyyy`
```

---

### 4-3. 버전 체인 확인

**사용자:**
```
버전 히스토리 보여줘
```

**도구 호출:** `get_versions`

```json
{ "entryId": "h_lam1_v2yyyy" }
```

**예상 응답:**
```
버전 체인
─────────────────────────────
v1  h_lam1_v1xxxx   18점  D   "AWS Lambda 함수 코드 작성해줘."
 └→ v2  h_lam1_v2yyyy   91점  A   "당신은 AWS Lambda + Go 전문가입니다..."
```

---

### 4-4. 두 버전 비교

**사용자:**
```
>> 비교
```

**도구 호출:** `compare_prompts`

```json
{
  "entryIdA": "h_lam1_v1xxxx",
  "entryIdB": "h_lam1_v2yyyy"
}
```

**예상 응답:**
```
버전 비교
─────────────────────────────
         v1 (D)    →    v2 (A)
점수      18       →    91      (+73 ▲)
clarity   15       →    92
complete  12       →    90
specific  18       →    95
context   20       →    88
output    25       →    90

텍스트 변화:
  + 페르소나 추가 ("당신은 ... 전문가입니다")
  + 런타임 버전 명시 (Go 1.21, provided.al2)
  + 에러 처리 방식 명시 (BatchItemFailures)
  + 환경변수 명시 (TABLE_NAME)
  + IAM 권한 범위 명시
  + 출력 형식 명시 (main.go + 테스트)
```

---

## 샘플 5. 자동 개선 (`>> fix`)

> Feature 1 구현 후 사용 가능 (API 키 필수)

**사용자:**
```
>> fix
```

내부적으로 직전 `entryId`의 분석 결과(`missingElements`)를 기반으로 Claude API가 개선된 프롬프트를 생성한다.

**예상 응답:**
```
자동 개선 생성
─────────────────────────────
원본 점수: 61 (C)  →  예상 개선 점수: 85+ (A)

개선된 프롬프트:
─────────────────────────────
당신은 Unity 2022 LTS C# 전문가입니다.
아래 요구사항에 맞는 플레이어 이동 시스템을 구현해주세요.

요구사항:
- 입력: WASD (Input System 패키지 사용)
- 물리: Rigidbody, FixedUpdate에서 처리
- 이동 속도: 5f (직렬화 가능하도록 [SerializeField] 사용)
- 벽 충돌: Physics 레이어 기반 LayerMask
- 플랫폼: PC 전용

컴포넌트 설계:
- InputHandler.cs: 입력 수신 담당
- PlayerMovement.cs: 물리 이동 담당 (InputHandler 의존성 주입)

출력: 두 파일의 전체 코드 + 각 컴포넌트 역할 주석
─────────────────────────────

변경 요약:
  추가: 페르소나, Unity 버전, Input System 명시
  추가: FixedUpdate 물리 기준 명시
  추가: 속도값 구체화 ([SerializeField])
  추가: 컴포넌트 분리 구조 명시
  추가: 출력 형식 (파일 2개 + 주석)

저장하려면 ">> fix+save" 를 입력하세요.
```

---

## 샘플 6. 자동 개선 루프 (`>> loop`)

> Feature 4 구현 후 사용 가능 (API 키 필수)

**사용자:**
```
>> loop 90
```

**예상 응답:**
```
자동 개선 루프 시작
목표: 90점 이상 / 최대 반복: 5회
─────────────────────────────
iter 1  h_loop_v1  →  61점 (C)  목표 미달, 계속...
iter 2  h_loop_v2  →  78점 (B)  목표 미달, 계속...
iter 3  h_loop_v3  →  92점 (A)  ✅ 목표 달성!

루프 완료: 3회 반복
최종 entryId: `h_loop_v3_zzzz`

최종 프롬프트:
─────────────────────────────
[92점 달성 프롬프트 전문]
─────────────────────────────

버전별 점수 변화: 61 → 78 → 92
```

---

## 샘플 7. 배치 분석 (`batch_analyze`)

> Feature 5 구현 후 사용 가능

후보 프롬프트 3개를 한 번에 비교하는 시나리오.

**사용자:**
```
이 세 가지 프롬프트 버전을 한 번에 비교 분석해줘

버전 A: "번역해줘."
버전 B: "다음 한국어 텍스트를 영어로 번역해줘."
버전 C: "당신은 전문 기술 번역가입니다. 아래 한국어 텍스트를 영어로 번역하세요. 기술 용어는 원문 그대로 유지하고, 자연스러운 영어 표현을 사용하세요. 번역문만 출력하세요."
```

**도구 호출:** `batch_analyze`

```json
{
  "prompts": [
    "번역해줘.",
    "다음 한국어 텍스트를 영어로 번역해줘.",
    "당신은 전문 기술 번역가입니다..."
  ],
  "mode": "local",
  "saveAll": false
}
```

**예상 응답:**
```
배치 분석 결과 (3개 프롬프트)
─────────────────────────────
순위  점수   등급  entryId            프롬프트 요약
  1    88     A    h_bat_c_xxx        "당신은 전문 기술 번역가입니다..."
  2    52     C    h_bat_b_yyy        "다음 한국어 텍스트를 영어로..."
  3    15     D    h_bat_a_zzz        "번역해줘."

최고 점수: h_bat_c_xxx (88점, A등급)
평균 점수: 51.7점
```

---

## 샘플 8. 히스토리 검색 및 내보내기

### 8-1. 고급 검색

**사용자:**
```
점수 80점 이상이고 #coding 태그 붙은 것만 보여줘
```

**도구 호출:** `query_history`

```json
{
  "scoreMin": 80,
  "tags": ["#coding"],
  "sortBy": "score",
  "limit": 10
}
```

**예상 응답:**
```
검색 결과 (3건)
─────────────────────────────
entryId: `h_z9y8...`   91점  A  #coding #go  "당신은 Go 백엔드 코드 리뷰어..."
entryId: `h_lam1_v2..` 91점  A  #coding #aws "당신은 AWS Lambda + Go 전문가..."
entryId: `h_loop_v3..` 92점  A  #coding      "[루프 최종 버전]"
```

---

### 8-2. 프로젝트 스냅샷 및 내보내기

**사용자:**
```
지금 상태 스냅샷 찍어줘. 레이블은 "v1.1 완성"
```

**도구 호출:** `snapshot_project`

```json
{
  "projectId": "game-server-prompts",
  "label": "v1.1 완성"
}
```

**사용자:**
```
마크다운으로 내보내줘
```

**도구 호출:** `export_project`

```json
{
  "format": "markdown",
  "outputPath": "~/exports/game-server-prompts.md"
}
```

---

### 8-3. HTML 대시보드 시각화

**사용자:**
```
>> 시각화
```

**도구 호출:** `visualize_project`

**예상 응답:**
```
✅ 대시보드 생성 완료
경로: ~/.promptic/exports/game-server-prompts-dashboard.html

포함 내용:
  - 점수 분포 히스토그램
  - 버전별 점수 추이 라인 차트
  - 등급별 파이 차트
  - 태그 빈도 막대 차트
  - 최근 10개 엔트리 테이블
```

---

## 샘플 9. 전체 통계 확인

**사용자:**
```
전체 통계 보여줘
```

**도구 호출:** `get_stats`

**예상 응답:**
```
전체 통계
─────────────────────────────
프로젝트 수:    2개
총 프롬프트:    14개
평균 점수:      68.3점
최고 점수:      92점  (h_loop_v3)
최저 점수:      15점  (h_bat_a)

등급 분포:
  A (80+):  5개  (35.7%)
  B (60~):  3개  (21.4%)
  C (40~):  4개  (28.6%)
  D (~39):  2개  (14.3%)

트렌드: 최근 7일 평균 75.1점 ▲ (전주 대비 +8.3)
```

---

## 샘플 8. `>> last N` — 최근 N번째 항목 참조

entryId를 직접 조회하지 않고 세션 링 버퍼에서 바로 지목합니다.

### 8-1. 여러 번 분석 후 이전 항목 참조

**사용자 (1번째 분석):**
```
코드 리뷰해줘 >> 분석
```
→ `h_001` 저장 (점수 42, D)

**사용자 (2번째 분석):**
```
Go 함수 에러 처리 개선해줘 >> 분석
```
→ `h_002` 저장 (점수 58, C)

**사용자 (3번째 분석):**
```
JWT 인증 미들웨어를 Go로 작성해줘 >> 분석
```
→ `h_003` 저장 (점수 71, B)

**사용자:**
```
>> last 2
```

**예상 응답:**
```
2번째 이전 entryId `h_002` 를 parentId로 설정했습니다.
새 프롬프트를 입력해 주세요 (parentId: `h_002` 로 자동 연결됩니다).
```

→ 이후 새 프롬프트를 입력하면 `h_002`의 버전 체인으로 연결됩니다.

---

### 8-2. 에러 케이스

**사용자 (세션 시작 직후):**
```
>> last 3
```

**예상 응답:**
```
⚠️ 세션 내 3번째 이전 분석이 없습니다. (현재 1건 기록됨)
```

**사용자:**
```
>> last 15
```

**예상 응답:**
```
⚠️ N은 1~10 사이여야 합니다. (입력값: 15)
```

---

## 샘플 9. 개선 프롬프트 클립보드 출력 (enhancedPromptBlock)

점수 80 미만인 프롬프트를 분석하면 분석 결과 하단에 바로 복사해서 쓸 수 있는 개선 프롬프트 블록이 자동으로 출력됩니다.

점수 80 미만인 프롬프트를 분석하면 분석 결과 하단에 바로 복사해서 쓸 수 있는 개선 프롬프트 블록이 자동으로 출력됩니다.

### 8-1. 낮은 점수 프롬프트 분석

**사용자:**
```
다음 프롬프트 분석해줘: 코드 최적화 해줘 >> 분석
```

**도구 호출:** `analyze_prompt`

```json
{
  "prompt": "코드 최적화 해줘",
  "mode": "local"
}
```

**예상 응답:**
```
## 프롬프트 분석 결과

**종합 점수: 35/100  등급: D**

| 축 | 점수 | 게이지 |
|---|---|---|
| 명확성 (Clarity)       | 50  | `█████░░░░░ 50` |
| 완전성 (Completeness)  | 20  | `██░░░░░░░░ 20` |
| 구체성 (Specificity)   | 20  | `██░░░░░░░░ 20` |
| 컨텍스트 (Context)     | 20  | `██░░░░░░░░ 20` |
| 출력형식 (Output Fmt)  | 20  | `██░░░░░░░░ 20` |

**누락된 요소:**
- 역할/페르소나 정의
- 배경 및 컨텍스트 정보
- 출력 형식 명시
- 구체적 지시사항 또는 예시

**추천 태그:** #coding #needs-context

✨ 개선된 프롬프트 (복사해서 바로 사용하세요):
────────────────────────────────────────────────
당신은 소프트웨어 개발자입니다.

코드 최적화 해줘

출력 형식: 코드 블록(```언어명```)과 함께 사용 예시 포함으로 응답해 주세요.

참고 배경: [관련 배경 정보 또는 제약사항을 여기에 추가하세요]
────────────────────────────────────────────────
entryId: `h_20260325_001`

💡 프로젝트에 저장하려면 `set_active_project` 를 먼저 실행하세요.

entryId: `h_20260325_001`
```

### 8-2. 개선 프롬프트로 재분석

개선된 프롬프트에 배경 정보를 직접 채워 재분석하면 점수 향상 효과를 즉시 확인할 수 있습니다.

**사용자:**
```
다음 프롬프트 분석해줘 >> re:
당신은 소프트웨어 개발자입니다.

Go 언어로 작성된 HTTP 핸들러 함수의 성능을 최적화해줘. 현재 응답 시간이 200ms 이상으로 느립니다.

출력 형식: 코드 블록과 함께 before/after 비교 및 성능 개선 이유 설명으로 응답해 주세요.

참고 배경: AWS Lambda에서 실행 중이며 콜드 스타트 최소화가 중요합니다.
```

**결과:** 점수 35 → 82, 등급 D → A로 향상

---

## 샘플 10. 도메인 특화 프리셋 활용

Claude Desktop `/` 메뉴에서 선택할 수 있는 도메인 특화 프리셋 사용 예시입니다.

### 9-1. code-review 프리셋 — Go 코드 리뷰

**사용자 (/ 메뉴 → code-review 선택):**
```
language: Go
context: HTTP 미들웨어 함수, JWT 인증 처리 담당
```

**자동 생성된 프롬프트:**
```
당신은 Go 전문 시니어 개발자입니다.
다음 코드를 리뷰해 주세요:

HTTP 미들웨어 함수, JWT 인증 처리 담당

리뷰 항목:
1. 버그 및 잠재적 오류
2. 코드 가독성 및 네이밍
3. 성능 최적화 포인트
4. 보안 취약점
5. 개선 제안 (코드 예시 포함)

출력 형식: 항목별 번호 목록, 개선 코드는 코드 블록(```언어명```)으로 제시해 주세요.
```

**도구 호출:** `analyze_prompt` (위 프롬프트 자동 분석)

**예상 결과:**
```
종합 점수: 85/100  등급: A

✅ 누락된 요소 없음
추천 태그: #coding #review #well-defined
```

---

### 9-2. doc-writing 프리셋 — API 문서 작성

**사용자 (/ 메뉴 → doc-writing 선택):**
```
docType: REST API 문서
audience: 외부 파트너 개발자
```

**자동 생성된 프롬프트:**
```
당신은 기술 문서 전문 작가입니다.
REST API 문서를 작성해 주세요.
독자 대상: 외부 파트너 개발자

작성 기준:
- 명확하고 간결한 문장 사용
- 코드 예시와 다이어그램(Mermaid) 포함
- 목차(Table of Contents) 제공
- 초보자도 이해할 수 있는 설명 수준

출력 형식: Markdown 형식, 섹션별 헤더(##) 구분
```

**예상 결과:**
```
종합 점수: 88/100  등급: A
추천 태그: #writing #explain #well-defined
```

---

### 9-3. system-design 프리셋 — 실시간 알림 서비스 설계

**사용자 (/ 메뉴 → system-design 선택):**
```
system: 실시간 게임 매치메이킹 알림 서비스
constraints: AWS, Go, WebSocket, 동시 접속 10만 명
```

**자동 생성된 프롬프트:**
```
당신은 시스템 아키텍트입니다.
실시간 게임 매치메이킹 알림 서비스 시스템을 설계해 주세요.
제약 조건: AWS, Go, WebSocket, 동시 접속 10만 명

설계 항목:
1. 전체 아키텍처 개요 (Mermaid 다이어그램 포함)
2. 주요 컴포넌트와 역할
3. 데이터 흐름 및 API 인터페이스
4. 확장성 및 장애 대응 전략
5. 기술 스택 선택 이유

출력 형식: 섹션별 Markdown, 다이어그램은 Mermaid 코드 블록으로 제공
```

**예상 결과:**
```
종합 점수: 91/100  등급: A
추천 태그: #analysis #coding #well-defined
```

> 도메인 프리셋은 고품질 프롬프트 골격을 자동 생성하므로 처음부터 A등급 이상의 프롬프트를 작성하는 데 도움이 됩니다.

---

## 빠른 참조 — 트리거 단축 명령

| 트리거 | 동작 | 전제 조건 |
|--------|------|----------|
| `>> 분석` / `>> anz` | local 모드 분석 | — |
| `>> 정밀분석` / `>> deep` | deep 모드 분석 | API 키, parentId |
| `>> re` / `>> v2` | 직전 entryId를 parentId로 설정 | 세션 내 이전 분석 |
| `>> last N` | N번째 이전 entryId를 parentId로 설정 (1~10) | 세션 내 N개 이상 분석 |
| `>> fix` | 자동 개선 생성 | API 키 |
| `>> fix+save` | 자동 개선 + 히스토리 저장 | API 키 |
| `>> loop N` | 목표 점수 N까지 자동 루프 | API 키, `>> fix` 구현 |
| `>> 비교` / `>> diff` | 최근 두 엔트리 비교 | 엔트리 2개 이상 |
| `>> 시각화` / `>> viz` | HTML 대시보드 생성 | 활성 프로젝트 |

---

*작성일: 2026-03-25*
  